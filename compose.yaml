---
version: '3.3'
services:
  db:
    image: postgres:17.6
    ports:
      - "8532:5432"
    environment:
      POSTGRES_USER: ${DB_USERNAME:-template_db_user}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-template_db_password}
      POSTGRES_DB: ${DB_NAME:-template_db_dev}